# travel-extension

This folder contains a variant of the Romeo scraper adapted for the Travel/Explore grid (`#explore-grid`).

To load:
1. Open Chrome -> `chrome://extensions` and enable Developer mode.
2. Click "Load unpacked" and choose `Romeo-Extension/travel-extension`.
3. Open Romeo Explore/Travel page and use the popup to start scraping.
